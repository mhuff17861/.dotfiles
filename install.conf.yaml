- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshenv: zshenv
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc
    ~/.zpreztorc: zpreztorc
    ~/.zprezto: prezto
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.tmux: tmux
    ~/.config/i3/config: i3config

- shell:
  - mkdir -p ~/.config/i3
  - [git submodule update --init --recursive, Installing submodules]
  - ./fonts/install.sh
